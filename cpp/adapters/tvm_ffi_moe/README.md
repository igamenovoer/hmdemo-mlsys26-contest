# tvm-ffi-moe Adapter

The first adapter is generated into each profiler artifact runner from `cpp/src/artifact.cpp`. Keep adapter-specific source here if the generated runner is split into reusable adapter modules later.
