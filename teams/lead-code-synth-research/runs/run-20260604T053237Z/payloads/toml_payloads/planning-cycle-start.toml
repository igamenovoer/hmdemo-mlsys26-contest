schema_id = "lead-code-synth-research.email.planning-cycle-start"
schema_version = "1"
kind = "request"
run_id = "run-20260604T053237Z"
plan_revision = "cli-credential-assignment-stage-0002"
payload_id = "planning-cycle-start-run-20260604T053237Z"
sender_id = "human-operator"
receiver_id = "planner"
route_id = "operator-to-planner"

[payload]
cycle_intent = "Start the first live fused-MoE optimization planning cycle for moe_fp8_block_scale_ds_routing_topk8_ng8_kg4_e32_h7168_i2048. Planner should issue up to two distinct bounded CUDA Coder directions, request research when useful, and stop after one bounded planning turn."
trigger_source = "human-operator-start"
current_best_ref = "initial-baseline:solution/cuda/kernel.cu"
history_refs = ["teams/lead-code-synth-research/source/mlsys26-tech-report.pdf", "teams/lead-code-synth-research/execplan/specs/objective/objective.toml", "teams/lead-code-synth-research/execplan/specs/collab/collab-overview.md"]
operator_constraints = ["Scope is fused MoE only.", "Promotion requires Evaluator official-timing evidence.", "Planner owns current-best writes only after accepted Evaluator evidence.", "Each CUDA Coder performs one bounded attempt on a distinct direction.", "Agents should pick a spare local GPU dynamically and report waiting-for-GPU instead of waiting in chat.", "Non-GPU blockers retry up to three times, then report failure to Planner.", "Researcher should search local references and network sources concurrently by default.", "If NCU requires sudo, agents must read the password only from NCU_ROOT_PW and never persist it."]
