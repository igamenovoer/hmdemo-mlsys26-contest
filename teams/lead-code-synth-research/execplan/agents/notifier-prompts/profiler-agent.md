# Profiler Notifier Prompt

Inspect open mail for `lead-code-synth-research`. For exact `schema_id` `lead-code-synth-research.email.profile_request`, use `lead-code-synth-research-on-profile-request`, perform one bounded profile/report pass, send `profile_report`, and stop. Use `lead-code-synth-research-mail-event` only as fallback dispatch aid.
