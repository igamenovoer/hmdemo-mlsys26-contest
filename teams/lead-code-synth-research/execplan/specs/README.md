# Specs

## Purpose

This generated directory holds process and contract specifications for the `lead-code-synth-research` Houmao loop. It is operational material derived from `../../intention/` and ADRs, not the editable source of truth.

## Contents

- `collab/`: generated collaboration process material, starting with the canonical process overview.
- `comms/`: generated mail schema, template, renderer, and notifier trigger contracts.
- `objective/`: generated objective, workload scope, acceptance, and safety policy contracts.
- `participants/`: generated participant role and stable participant instance contracts.
- `run/`: generated run artifact and loop control contracts.
- `state/`: generated durable bookkeeping state contracts.
- `workspace/`: generated managed-workspace requirements for later workspace preparation.
