# Generated objective contract.
loop_slug = "lead-code-synth-research"
kernel_scope = "Fused MoE"
objective = "open-ended-speedup-maximization"
acceptance_authority = "synthesizer"
terminal_authority = "operator"

[success_posture]
description = "Maximize Fused MoE speedup until operator stop, budget exhaustion, or concrete blocker."
automatic_stop_on_report_reproduction = false
automatic_stop_on_baseline_beaten = false
automatic_stop_on_promotion = false

[promotion_rule]
description = "A candidate may become current-best only when it is correct, complete, and faster."
requires_correctness_pass = true
requires_complete_evidence = true
requires_timing_improvement = true
baseline = "previous current-best for the active run"

[run_inputs]
description = "Operator-provided values required before start."
workload_scope = "UNRESOLVED - bind during run setup"
allowed_edit_surface = "UNRESOLVED - bind during run setup"
correctness_command = "UNRESOLVED - bind during run setup"
timing_command = "UNRESOLVED - bind during run setup"
profiling_command = "UNRESOLVED - optional unless planner requests profiler evidence"
initial_current_best = "UNRESOLVED - bind during run setup"
