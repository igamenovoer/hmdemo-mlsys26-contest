```houmao-email-metadata
schema_id = "lead-code-synth-research.email.candidate_result"
schema_version = "1"
kind = "reply"
run_id = "{{ run_id }}"
plan_revision = "{{ plan_revision }}"
payload_id = "{{ payload_id }}"
handoff_id = "{{ handoff_id }}"
work_item_id = "{{ work_item_id }}"
search_direction_id = "{{ search_direction_id }}"
sender = "{{ sender }}"
receiver = "{{ receiver }}"
```

# Candidate Result

Attempt: {{ attempt_index }}

Context: {{ context }}

Evidence: {{ evidence }}

Body: {{ body }}
