```houmao-email-metadata
schema_id = "{{ schema_id }}"
schema_version = "{{ schema_version }}"
kind = "{{ kind }}"
run_id = "{{ run_id }}"
plan_revision = "{{ plan_revision }}"
payload_id = "{{ payload_id }}"
sender_id = "{{ sender_id }}"
receiver_id = "{{ receiver_id }}"
route_id = "{{ route_id }}"
evaluation_id = "{{ payload.evaluation_id }}"
candidate_id = "{{ payload.candidate_id }}"
```

# Evaluation Report

## Decision

- Candidate id: {{ payload.candidate_id }}
- Variant ref: {{ payload.project_cli_variant_ref }}
- Decision: {{ payload.decision }}
- Correctness status: {{ payload.correctness_status }}
- Official timing provenance: {{ payload.official_timing_provenance | default('none') }}
- Speedup or latency summary: {{ payload.speedup_or_latency_summary | default('none') }}

## Anti-Hacking Review

{{ payload.anti_hacking_review }}

## Rejection Reason

{{ payload.rejection_reason | default('none') }}

## Requested Action

Planner should write current-best only when this report is accepted promotion evidence, otherwise record rejection context and decide the next cycle or operator escalation.
