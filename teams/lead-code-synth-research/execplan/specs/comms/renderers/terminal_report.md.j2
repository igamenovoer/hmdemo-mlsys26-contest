```houmao-email-metadata
schema_id = "lead-code-synth-research.email.terminal_report"
schema_version = "1"
kind = "report"
run_id = "{{ run_id }}"
plan_revision = "{{ plan_revision }}"
payload_id = "{{ payload_id }}"
handoff_id = "{{ handoff_id }}"
sender = "{{ sender }}"
receiver = "{{ receiver }}"
```

# Terminal Report

Terminal reason: {{ terminal_reason }}

Context: {{ context }}

Body: {{ body }}
