```houmao-email-metadata
schema_id = "lead-code-synth-research.email.profile_request"
schema_version = "1"
kind = "request"
run_id = "{{ run_id }}"
plan_revision = "{{ plan_revision }}"
payload_id = "{{ payload_id }}"
handoff_id = "{{ handoff_id }}"
sender = "{{ sender }}"
receiver = "{{ receiver }}"
```

# Profile Request

Context: {{ context }}

Body: {{ body }}
