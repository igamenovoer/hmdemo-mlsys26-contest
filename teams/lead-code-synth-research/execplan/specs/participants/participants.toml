# Generated participant contract.
loop_slug = "lead-code-synth-research"

[[roles]]
role = "planner"
description = "Owns search direction, task assignment, run history, and operator escalation."
instances = ["planner"]

[[roles]]
role = "coder"
description = "Implements one bounded Fused MoE CUDA task in an isolated workspace and reports evidence."
instances = ["coder-1", "coder-2", "coder-3"]

[[roles]]
role = "synthesizer"
description = "Reviews candidate results and promotes faster correct candidates to current-best."
instances = ["synthesizer"]

[[roles]]
role = "profiler"
description = "Produces NCU and benchmark-derived bottleneck reports."
instances = ["profiler"]

[[roles]]
role = "researcher"
description = "Provides external references and optimization briefs after stalls."
instances = ["researcher"]

[[roles]]
role = "operator"
description = "Human runtime authority for stop, override, repair, budget, and search correction."
instances = ["operator"]
agent_binding = "none"
