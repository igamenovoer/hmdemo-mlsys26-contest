# Harness Schemas

## Purpose

This generated directory contains schemas owned by the harness rather than by objective, communication, state, workspace, or topology contracts.

## Contents

- `command-envelope.schema.json`: JSON Schema for machine-readable harness command output.
