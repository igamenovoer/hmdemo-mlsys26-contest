# Claude Code Instructions

Read `AGENTS.md` first. It is the shared project instruction file for Codex and Claude Code.

OpenSpec support for Claude Code is installed under `.claude/skills/openspec-*`.
